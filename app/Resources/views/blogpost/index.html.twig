{% extends 'base.html.twig' %}

{% block body %}
    <h1>Blogposts list</h1>

    <ul>
        <li>
            <a href="{{ path('blogpost_new') }}">Create a new blogPost</a>
        </li>
    </ul>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>Urlalias</th>
                <th>Published</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for blogPost in blogPosts %}
            <tr>
                <td><a href="{{ path('blogpost_show', { 'id': blogPost.id }) }}">{{ blogPost.id }}</a></td>
                <td>{{ blogPost.title }}</td>
                <td>{{ blogPost.content }}</td>
                <td>{{ blogPost.urlAlias }}</td>
                <td>{% if blogPost.published %}{{ blogPost.published|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('blogpost_show', { 'id': blogPost.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('blogpost_edit', { 'id': blogPost.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
